## -*- coding: utf-8 -*-

#from uuid import uuid4

#from django.db.models import (
    #Model, ForeignKey, DateTimeField, NullBooleanField, ManyToManyField, 
    #TextField, CharField, PositiveSmallIntegerField
#)
#from django.db.models import JSONField
#from django.utils.timezone import now

#

#from . import base 
#from .appform import FORM_TYPE


    
#class Channel(Model):
    #"""
    #"""
    #update = base.update
    
    #id = CharField(max_length=50, default=uuid4, primary_key=True)
    #time_created = DateTimeField(default=now, verbose_name='Время создания записи в БД на сервере')

    #name = TextField()
    #users = ManyToManyField(User)
    #admins = ManyToManyField(User)
    #organization = FK(Coordinator)
    #ievent = FKAnswer
    #icon_url = TextField()
    
    
